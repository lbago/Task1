<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="androidlab.epam.com.task1">
    <uses-sdk android:targetSdkVersion="22"/>
    <uses-permission android:name="android.permission.INTERNET" /> //доступ в интернет
    <uses-permission android:name="android.permission.CAMERA" /> // доступ к камере, возможность делать фото и видео
    <uses-permission android:name="android.permission.RECORD_AUDIO" /> // доступ к микрофону
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" /> //доступ к карте, читать файлы пользователя

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>